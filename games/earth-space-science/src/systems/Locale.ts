import { createLocale } from '@shared/locale';

export const { getLang, setLang, toggleLang, detectDefaultLang } = createLocale('earth-space-science');
export type { LangMode } from '@shared/locale';
