import { createLocale } from '@shared/locale';

export const { getLang, setLang, toggleLang, detectDefaultLang } = createLocale('advanced-trigonometry');
export type { LangMode } from '@shared/locale';
