import { createLocale } from '@shared/locale';

export const { getLang, setLang, toggleLang, detectDefaultLang } = createLocale('number-theory-logic');
export type { LangMode } from '@shared/locale';
